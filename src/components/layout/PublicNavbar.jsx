import {useState} from 'react';
import {Link,useNavigate} from 'react-router-dom';
import {Bookmark,Menu,X,ChevronDown} from 'lucide-react';
import Logo from '../common/Logo';
import {useAuth} from '../../context/AuthContext';
export default function PublicNavbar(){const [open,setOpen]=useState(false);const {user}=useAuth();const nav=useNavigate();return <header className="public-nav"><Logo/><button className="icon-btn mobile-toggle" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button><nav className={open?'nav-links open':'nav-links'}><Link to="/warehouses">Warehouses</Link><Link to="/logistics">Logistics</Link><Link to="/how-it-works">How it works</Link><Link to="/verification">Verification</Link><Link to="/partners">For partners <ChevronDown size={14}/></Link><Link to="/plans">Subscription</Link><Link to="/help">Help</Link></nav><div className="nav-actions"><Link className="saved-link" to="/saved"><Bookmark size={19}/><span>Saved</span></Link>{user?<button className="btn btn-dark" onClick={()=>nav(`/${user.role.toLowerCase().replaceAll('_','-')}/dashboard`)}>Dashboard</button>:<><Link className="btn btn-ghost" to="/login">Login</Link><Link className="btn btn-primary" to="/register">Register</Link></>}</div></header>}
